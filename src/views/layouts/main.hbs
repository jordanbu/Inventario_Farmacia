<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Farmacia Admin</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/zephyr/bootstrap.min.css">
    <link rel="stylesheet" href="/main.css"> <!-- Enlace a tu archivo CSS personalizado -->
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Farmacia Admin</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <!-- Usa el helper 'ifeq' para aplicar la clase 'active' si la ruta actual es '/' -->
                        <a class="nav-link {{#ifeq request.path '/'}}active{{/ifeq}}" href="/">Productos</a>
                    </li>
                    <li class="nav-item">
                        <!-- Usa el helper 'ifeq' para aplicar la clase 'active' si la ruta actual es '/ventas' -->
                        <a class="nav-link {{#ifeq request.path '/ventas'}}active{{/ifeq}}" href="/ventas">Ventas</a>
                    </li>
                    <li class="nav-item">
                        <!-- Usa el helper 'ifeq' para aplicar la clase 'active' si la ruta actual es '/historial-ventas' -->
                        <a class="nav-link {{#ifeq request.path '/historial-ventas'}}active{{/ifeq}}" href="/historial-ventas">Historial de Ventas</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container p-4">
        <div class="row">
            {{{body}}} <!-- Aquí se inyecta el contenido de las otras vistas (index.hbs, ventas.hbs, historial-ventas.hbs) -->
        </div>
    </div>

    <!-- Script de Bootstrap, necesario para el navbar responsive y otros componentes -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
